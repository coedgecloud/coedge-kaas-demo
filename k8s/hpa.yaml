apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: demo-app-hpa
  namespace: customer-52410
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: demo-app
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 1   # Low threshold so HPA triggers easily in testing
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 10   # Scale up fast for testing
      policies:
      - type: Pods
        value: 2
        periodSeconds: 30
    scaleDown:
      stabilizationWindowSeconds: 120  # Wait 2 min before scaling down
      policies:
      - type: Pods
        value: 1
        periodSeconds: 60
